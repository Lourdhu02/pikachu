[project]
name = "obb-engine"
version = "0.1.0"
description = "YOLO26-OBB training engine for dial detection"
requires-python = ">=3.10"

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.mypy]
python_version = "3.10"
strict = false
ignore_missing_imports = true
